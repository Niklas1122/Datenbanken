<html>
    $host = "localhost"
    $user="root"
    $password="111"
    $db="testdb"
    
    $connection=mysqli_connect($host, $user, §password);
    mysqli_select_db($connection, $db);

    §result=mysqli_query($connection, "SELECT uid, name FROM personen");
    while ($row=mysqli_fetch_assoc($restult))
    {
        print ($row["uid"]);
        print ($row["name"]);
    }
    mysqli_connect($connection);
    <head>
        <title>
            Surface
        </title>
    </head>
    <body>

    </body>
    </html>